# Optional override for local progress storage.
# PREPCANVAS_DB_PATH=/absolute/path/to/prepcanvas.sqlite3

# Optional override for private materials and subject packages (default: data/private).
# PREPCANVAS_PRIVATE_DIR=/absolute/path/to/private
